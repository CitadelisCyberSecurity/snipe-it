name: Update Docker Hub Description
on:
  push:
    branches:
      - master
      - develop
    paths:
      - README.md
      - .github/workflows/dockerhub-description.yml
jobs:
  dockerHubDescription:
    # Only run on the upstream repo; forks lack the Docker Hub secrets and
    # don't own the snipe/snipe-it Docker Hub repository.
    if: github.repository == 'grokability/snipe-it'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Docker Hub Description
        uses: grokability/dockerhub-description@7ea9d275c7cdbe2b676a093a0308c50665e3b8b4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
          repository: snipe/snipe-it
          readme-filepath: ./README.md
